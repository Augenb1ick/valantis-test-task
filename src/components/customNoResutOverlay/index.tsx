import { Stack } from '@mui/material';

const customNoResutOverlay = () => {
    return (
        <Stack height='100%' alignItems='center' justifyContent='center'>
            Нет продуктов с таким фильтром
        </Stack>
    );
};

export default customNoResutOverlay;
